# Local dev — copy to .env and fill in values
# GOOGLE_CLOUD_PROJECT is needed locally; Cloud Run sets it automatically
GOOGLE_CLOUD_PROJECT=your-project-id

# Skip Pub/Sub locally and POST directly to a running article-processor
ARTICLE_PROCESSOR_URL=http://localhost:8082

PORT=8081
